# Core dependencies for GUI and media processing
PySide6>=6.0
av
numpy
matplotlib
Pillow
opencv-python>=4.5

# Optional / extras
# OCR support (requires Tesseract installed on the system):
pytesseract  # optional

# Optional: NDI support requires the NewTek NDI SDK and a Python binding
# (not always available via PyPI). See NDI_README.md for details.
